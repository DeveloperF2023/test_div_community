name: test_div_community
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  google_fonts:
  flutter_bloc:
  dartz:
  dio:
  equatable:
  intl:
  iconify_flutter:
  get_it:
  shimmer:
  url_launcher:
  pdf:
  printing:
  path_provider:
  permission_handler:
  flutter_local_notifications:
  cached_network_image:   


dev_dependencies:
  flutter_file_dialog:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0



flutter:


  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
